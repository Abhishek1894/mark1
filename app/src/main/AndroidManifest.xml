<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mark1">


    <!-- following code is added while upiPayment fragment -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/apt"
        android:label="@string/app_name"
        android:roundIcon="@drawable/apt"
        android:supportsRtl="true"
        android:theme="@style/Theme.Mark1">
        <activity
            android:name=".RazorpayPayment"
            android:exported="false" />
        <activity
            android:name=".RegisterApartment"
            android:exported="false" />
        <activity
            android:name=".JoinApartmentActivity"
            android:exported="false" />
        <activity
            android:name=".LoginActivity"
            android:exported="false" />
        <activity
            android:name=".HomeActivity"
            android:exported="false" />
        <activity
            android:name=".OptionsActivity"
            android:exported="false" />
        <activity
            android:name=".SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>